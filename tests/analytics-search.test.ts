import {test} from 'node:test';import assert from 'node:assert/strict';import {analyticsSearchSnapshot,clickedSearchResult} from '../src/analytics/search.ts';
const search={status:'ready' as const,query:'转出',page:2,pages:2,total:21,results:[{id:'a',revision:1,title:'正文不得进入事件',href:'/help-centre?article=a',breadcrumbs:[],snippet:'不保存摘要'}]};
test('search snapshot strips titles snippets and URLs while preserving authorized order',()=>{const snapshot=analyticsSearchSnapshot(search,'id');assert.deepEqual(snapshot,{eventId:'id',query:'转出',page:2,total:21,results:[{documentId:'a',revision:1}]});assert.equal(analyticsSearchSnapshot({...search,status:'invalid'},'id'),null);assert.equal(analyticsSearchSnapshot({...search,results:[{...search.results[0],revision:undefined}]},'id'),null);});
test('search clicks map only to actual result links with global result positions',()=>{assert.deepEqual(clickedSearchResult(search,'http://local/help-centre?article=a','http://local'),{documentId:'a',revision:1,position:21});for(const href of ['/help-centre?q=转出&page=2','https://other.test/help-centre?article=a','/help-centre?article=hidden'])assert.equal(clickedSearchResult(search,href,'http://local'),null);});
