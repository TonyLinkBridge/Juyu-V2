import {readFile,writeFile,mkdir,readdir} from 'node:fs/promises';import {resolve,dirname} from 'node:path';import {createRequire} from 'node:module';import ts from 'typescript';
const require=createRequire(import.meta.url);
import type {AvailabilityDetail} from '../../src/availability/model';
export const availabilityFixture:AvailabilityDetail={documentId:'availability-local',title:'客服异常处理流程',sequence:5,revision:2,status:'in_review',lifecycle:'active',publishedRevision:1,canArchive:true,canUnpublish:true,canUnarchive:false,history:[],historyMore:false};
export async function availabilityBrowserBundle(){const dir=resolve('output/verification/availability-fixture');await mkdir(dir,{recursive:true});for(const name of ['components/availability/AvailabilityPanel.tsx','availability/client.ts','components/availability/ArchivesList.tsx']){const p=resolve(dir,name.replace(/\.tsx?$/,'.js'));await mkdir(dirname(p),{recursive:true});await writeFile(p,ts.transpileModule(await readFile(`src/${name}`,'utf8'),{compilerOptions:{jsx:ts.JsxEmit.ReactJSX,module:ts.ModuleKind.ESNext,target:ts.ScriptTarget.ES2022}}).outputText);}
 await writeFile(resolve(dir,'entry.js'),`import React from 'react';import {createRoot} from 'react-dom/client';import {AvailabilityPanel} from './components/availability/AvailabilityPanel';import {ArchivesList} from './components/availability/ArchivesList';const initial=JSON.parse(document.getElementById('data').textContent);createRoot(document.getElementById('root')).render(document.body.dataset.list==='1'?React.createElement(ArchivesList,{data:initial}):React.createElement(AvailabilityPanel,{initial}));`);
 const {webpack}=require('next/dist/compiled/webpack/webpack');await new Promise<void>((done,reject)=>{const compiler=webpack({mode:'development',devtool:false,entry:resolve(dir,'entry.js'),output:{path:dir,filename:'bundle.js'},resolve:{modules:[resolve('node_modules')]},module:{rules:[{test:/\.m?js$/,resolve:{fullySpecified:false}}]}});compiler.run((error:Error|null,stats:{hasErrors():boolean;toString():string})=>compiler.close(()=>error||stats.hasErrors()?reject(error??new Error(stats.toString())):done()));});
 const css=(await Promise.all((await readdir('.next/static/chunks')).filter(f=>f.endsWith('.css')).map(f=>readFile(resolve('.next/static/chunks',f),'utf8')))).join('\n');return {css:css+await readFile('src/app/globals.css','utf8'),script:await readFile(resolve(dir,'bundle.js'),'utf8')};}
