import {readBounded} from '../media/upload.ts';
export async function readFieldBody(request:Request):Promise<unknown>{try{const bytes=await readBounded(request.body,16000,AbortSignal.any([request.signal,AbortSignal.timeout(15000)]));return JSON.parse(new TextDecoder('utf-8',{fatal:true}).decode(bytes));}catch{throw new Error('INVALID_INPUT');}}
export async function fieldsResponse(action:()=>Promise<unknown>){const headers={'Cache-Control':'private, no-store',Vary:'Cookie, Authorization'};try{return Response.json(await action(),{headers});}catch(e){const code=e instanceof Error?e.message.split(':')[0]:'';const status=code==='FORBIDDEN'?403:code==='INVALID_INPUT'?400:['FIELD_CONFLICT','FIELD_LIMIT'].includes(code)?409:503;return Response.json({error:status===503?'FIELDS_UNAVAILABLE':code},{status,headers});}}
