import {readBounded,requireMediaOrigin} from '../media/upload.ts';
export async function readFeatureBody(request:Request){requireMediaOrigin(request,process.env.APP_ORIGIN);if(request.headers.get('content-type')?.split(';')[0].trim()!=='application/json')throw new Error('INVALID_INPUT');try{return JSON.parse(new TextDecoder('utf-8',{fatal:true}).decode(await readBounded(request.body,4096,AbortSignal.any([request.signal,AbortSignal.timeout(15000)]))));}catch{throw new Error('INVALID_INPUT');}}
export async function featureResponse(action:()=>Promise<unknown>){const headers={'Cache-Control':'private, no-store',Vary:'Cookie, Authorization'};try{return Response.json(await action(),{headers});}catch(e){const code=e instanceof Error?e.message.split(':')[0]:'';const status=code==='FORBIDDEN'||code==='FEATURE_DISABLED'?403:code==='INVALID_INPUT'?400:code==='FEATURE_CONFLICT'?409:503;return Response.json({error:status===503?'FEATURE_UNAVAILABLE':code},{status,headers});}}
