# T063 · 全项目一致性、可用性与性能整改

2026-09-12 用户批准。一个总任务，24 个子项；本任务在当前会话顺序执行，不使用 subagent。

状态：[ ] 未开始；[~] 处理中或仍有验收缺口；[x] 该项全部验收完成。代码完成、本地验证、正式站验收分别记录；不能以本地测试替代线上完成。

## 范围与原则

- 按用户提供的 GitBook 源码及已选第三种文章风格实施，保留已批准的首页、登录及编辑器设计。
- 保留所有服务端角色、发布版本、附件和导出权限检查。
- 不创建或更改正式业务内容；不因为空数据用示例冒充正式资料。
- 只读审查记录不是本轮复验结果；第 24 项是待测量疑点。
- 不重复计算已修好的收藏对齐、文章底部后台入口和资料目录重定向。

## 子项与验收

| 编号 | 状态 | 问题 | 验收标准 |
|---|---|---|---|
| R01 | [~] | 搜索页重复侧栏 | 只保留一套导航；结果区桌面和手机都不被挤压。 |
| R02 | [~] | 搜索里的 Q&A 类型混淆 | 问答不进文章目录；类型、数量、入口分别正确。 |
| R03 | [~] | 搜索页重复后台入口和错误开通提示 | 移除重复入口；二审配置提示放后台并核实实际管理员状态。 |
| R04 | [~] | 分类入口多出中间页 | 按授权分类树进入阅读；空分类和失效链接有明确反馈。 |
| R05 | [~] | 手机顶栏拥挤 | 390px 下搜索和账号可操作；主题及后台入口收进菜单。 |
| R06 | [~] | OPS 阅读结构不一致 | 独立权限与目录树，直接阅读已发布 OPS 内容。 |
| R07 | [~] | Reference 多层入口和空状态 | 选择即显示速查表；无资料时不要求选择不存在的资料。 |
| R08 | [~] | 后台侧栏缺少 Reference | 增加对等入口；不误称现有模块标签不可访问。 |
| R09 | [~] | 后台分类层级不清楚 | 树形展示主分类、子分类及操作；说明贴近相应字段。 |
| R10 | [~] | 设置仍有系统确认弹窗 | 统一站内确认；保留浏览器关闭页面的原生离开保护。 |
| R11 | [~] | Q&A 重开重复读取 | 同页已读答案可复用；身份失效、权限或版本变化时不继续展示旧内容。 |
| R12 | [~] | 取消收藏刷新整页 | 仅更新条目和数量；失败可恢复，并提示原因。 |
| R13 | [~] | 收藏和最近浏览跳错模块 | 按文章、OPS、Q&A、Reference 类型直接到正确入口。 |
| R14 | [~] | 反馈首次额外读取 | 减少重复读取；按成员及正式版本隔离，并验证更新一致性。 |
| R15 | [~] | 标题与目录生成说明不足 | 清楚提示真正的标题会生成目录；不擅自改写已发布正文。 |
| R16 | [~] | 正式 PDF 下载失败 | 云端包含可运行浏览器和中文字体；真实授权下载成功，导出前后权限复核保留。 |
| R17 | [~] | Reference 表格格式丢失 | 颜色、背景、粗体、链接等与正文一致；文本搜索/筛选仍正确。 |
| R18 | [~] | 审核及发布离开保护不完整 | 退回原因、改派、发布操作中的输入和未确定结果得到离开/重试保护。 |
| R19 | [~] | 编辑器关闭后无法恢复未保存输入 | 按账号与文章隔离的受控恢复；退出和共享设备隐私边界明确。 |
| R20 | [~] | PDF 勾选项格式不同 | 完成项的勾选、删除线、颜色、链接在 PDF 与阅读页一致。 |
| R21 | [~] | PDF 屏幕顶部缺少全站操作 | 屏幕保留账号/退出/主题/导航；打印纸张使用独立干净样式。 |
| R22 | [~] | 编辑每次加载全部历史 | 日常读写只取所需版本；历史按需加载，版本和审计完整性不变。 |
| R23 | [~] | 不同文章保存争用全局锁 | 缩小锁范围并测试并发；分类/字段变更的一致性仍有保护。 |
| R24 | [~] | 附件身份校验可能重复 | 先计时统计，再合并安全的重复工作；交付前后权限复核不能取消。 |

## 顺序

1. R16：PDF 云端运行环境。
2. R17、R20：Reference 和 PDF 格式保真。
3. R18、R19：审核、发布和编辑输入保护。
4. R01–R04、R06–R08、R13：搜索与各模块阅读/导航统一。
5. R05、R09、R10、R15、R21：手机、设置、提示和 PDF 顶栏。
6. R11、R12、R14：消除不必要的刷新和重复请求。
7. R22–R24：历史、并发锁和媒体权限链路的性能；测量再调整。

## R16 实施计划

- 检查 src/server/pdf/chromium.ts、src/server/science/render.ts 与 next.config.ts 的浏览器安装、启动及追踪。
- 新增统一的服务端浏览器启动配置：本机保留 Playwright，Vercel Linux 使用随包附带的无头 Chromium。
- 带入本地中文字体，渲染时禁止向第三方请求正文、附件或字体。
- 先写运行环境分流回归测试；再修改 PDF 和流程图的共享依赖。
- 运行定向单元、真实 PDF、类型、lint 和生产构建；检查 PDF/流程图部署清单包含所需文件，普通页面不携带浏览器大包。
- 生产验收：部署后用真实授权文章下载，检查中文、多页、图片、颜色与导出期间权限变化；未部署时保持 [~]。

## 执行记录

- 2026-09-12：创建任务；T059 部署说明与当前源码均显示此前只追踪 Playwright 程序，缺少 Vercel 可运行浏览器。精确线上失败日志尚未拿到，补齐该缺口后仍需正式站复验。

### R16 本地结果（2026-09-12）

- 已加入固定版本 @sparticuz/chromium 153.0.0 及 Google Fonts Noto Sans SC（OFL 许可）。Vercel Linux 使用随包浏览器，本机继续使用 Playwright；不继承误填的本机可执行路径。
- PDF 与流程图共用运行环境；不复用私人页面或输出。离线渲染、生成前后权限/版本/附件复核保留。
- 已添加 4 个环境分流测试及自动部署清单检查；构建若缺少浏览器或中文字库会失败。
- 相关规则 15/15、真实本地 PDF/流程图 10/10、lint、生产构建（含 TypeScript）通过。构建最初暴露测试环境参数类型问题，已修正并重新通过。
- 4 个 PDF/流程图接口均追踪到完整浏览器资源和中文字体；PDF 接口文件清单约 118.1 MiB。帮助中心页面约 9.0 MiB，未带入浏览器/中文字体大文件。这是 Next 文件清单大小，不是 Vercel 最终包或响应大小。
- 尚未推送、部署或完成 Linux 云端真实下载；本机不能执行 Linux x64 二进制。Chromium 153 与现有 Playwright 1.62.1 的实际云端组合、中文字体显示及冷启动耗时仍须上线复验。R16 保持 [~]，不能记为生产已修复。
- 下一子项：R17 Reference 表格保留格式和链接。R16 线上验收作为独立待验项保留。

- 2026-09-12 R17 开始：确认 repository 使用 tableTextGrid 丢弃富文本；将保留经过校验的表格正文，复用员工阅读的行内格式渲染，单独保留纯文本用于筛选。

### R17 本地结果（2026-09-12）

- 已完成原生表格格式投影及共享行内显示；颜色、背景、文字标记、链接、对齐、列宽和合并单元格保留。
- 筛选和分页处理合并单元格的连续/非连续可见行，保留来源标签与原行号。普通旧表格兼容。
- 35 项专项测试、lint、类型及生产构建通过；桌面浅色和手机深色截图已查看。
- 不需要 SQL 迁移；没有修改正式资料。尚未推送、部署或进行真实账号线上验收，保持 [~]。
- 详细记录：docs/verification/2026-09-12-t063-reference-formats.md。
- 下一子项：R20，修复 PDF 已完成勾选项缺少删除线，检查相关格式与阅读页一致性。

### R20 本地结果（2026-09-12）

- 根因：printEditor 原来只输出 ☑/☐，没有像员工阅读页一样给已完成项文字加删除线。
- 已让完成状态只装饰当前项目的文字和链接；未完成的嵌套子项目不受影响。文字原有颜色、背景、粗体、斜体、下划线及手动删除线继续保留。
- 回归先失败再通过：15 项格式/导出权限规则；11 项实际 PDF/流程图，共 26 项通过。lint、TypeScript 和生产构建通过。
- 实际生成 output/pdf/T063-R20-checklist.pdf，并用 Poppler 渲染 PNG 人工查看：中文、已完成长文字换行删除线、未完成子项目、红字背景与手动格式正常。pypdf 确认一页内容完整且规则链接仍有可点击注释。
- 未改真实文章、审核状态或权限检查。未推送/部署；正式站下载仍需 R16 部署后联合验收。R20 保持 [~]，本地完成不代表线上关闭。
- 下一子项：R18，审核/改派/发布页面的未保存输入及未确定操作结果的离开保护。


### R18 本地结果（2026-09-12）

- 审核、撤回/改派、发布共用离开保护；未提交的原因或选择在切换链接、重新读取、退出时先确认，取消保留输入。忙碌状态阻止页面内离开；未知结果保留原请求重试，避免重构新操作。
- 已确认成功后解除保护；成功后状态读取失败仍保留成功回执，不误报操作失败。用户明确退出未知结果页面前说明丢失重试信息的后果；退出失败恢复保护。
- 文档关闭/刷新使用浏览器原生 beforeunload 提醒；支持 Navigation API 的浏览器后退/前进也受保护，页内锚点不拦截。旧浏览器没有 Navigation API 时的 SPA 后退仍未覆盖，Safari 真机与线上 Clerk 退出尚未验收。
- 先复现全局侧栏直接丢输入，再修复通过。68 项桌面/手机 Chromium 浏览器回归、3 项退出注册保护测试通过，lint、生产构建/TypeScript 和构建资源检查通过。浏览器回归使用隔离样例，不执行真实审核/发布；未配置服务专用测试不适用于当前本地配置，本次未运行。
- 已检查 output/verification/R18-leave-desktop.png 与 R18-leave-mobile.png：站内确认弹窗文字与按钮正常。未改服务端权限、正式版本、数据库或线上内容。
- 本地实现完成，未推送/部署；R18 保持 [~]，待真实账号及目标浏览器验收。强制关闭或崩溃后的数据恢复由 R19 处理。
- 下一子项：R19 编辑器受控草稿恢复，明确账号/文章隔离及退出后的隐私边界。


### R19 本地实现（2026-09-12）

- 默认关闭，管理员可主动启用本机恢复；通过服务端验证的账号 ID 和文章 ID 分隔副本。副本为未加密浏览器数据，不能当作共享设备上的安全隔离。24 小时内可恢复，过期后再次打开编辑器时清理。
- 保存当前输入（含 BlockNote 正文与设置），正常保存成功后清除当前副本；原文章恢复前检查服务器 sequence、状态及权限响应。冲突时保留副本供人工对照，不覆盖服务器。恢复后自动保存暂停，需主动保存。
- 未首次保存的新文章保留原 UUID；恢复前确认服务器尚无此文章，避免响应丢失后另建重复文章。其他页不同副本会触发停止覆盖提醒；尚未完成高并发标签页竞态验收。
- 正常退出清除所有本机恢复副本；存储异常不会阻止 Clerk 退出，但会提示用户清理网站数据。退出的跨标签页状态更新停止后续副本写入。浏览器崩溃不会执行清理，这是恢复所需边界；自动会话过期不等同于主动退出。
- 本机存储失败明确显示失败，不假称有副本。磁盘清理、隐私模式或浏览器拒绝存储仍可能无法恢复。
- 扩大旧 editor.spec 回归时发现旧按钮、旧确认框和旧选择器未同步；该运行 3 项通过、10 项失败、1 项中断、44 项未执行，不能声称全套编辑器通过。已保留需要更新旧测试的验收缺口，不把这些失败当作已修复。
- 未推送/部署或执行正式文章修改；真实 Safari、真实 Clerk 退出及正式账号恢复待验收。

- 最终专项验证：20 项当前桌面/手机 Chromium 编辑、格式保存及恢复流程，4 项本机存储规则及 3 项离开保护规则通过（27 项）；lint、TypeScript/生产构建通过。手机原生粘贴测试的截图定位已从隐藏编辑区改为可见预览区，重新通过。
- 已查看桌面/手机恢复提示截图：文字、恢复与放弃按钮可见，没有横向溢出。正常状态下说明折叠，发现副本时展开。
- R19 保持 [~]：实现与专项验证完成，旧编辑器回归测试同步、真实登录退出/设备恢复验收未完成。下一步先补齐旧测试与现有界面的差异，再继续 R01 搜索页框架整改。


### R19 编辑器回归补验（2026-09-12）

- 已同步旧编辑器测试到当前真实交互：原生斜杠菜单、设置抽屉、站内确认弹窗、输入完整文章标题删除，以及 Q&A 的问题/分类/排序字段。未删除业务断言或跳过旧失败项。
- 验证包括保存中的继续输入、未知回执原操作重试、版本冲突不覆盖、服务器内容恢复并保留输入、审核锁定、删除后冻结、OPS 阅读范围、Reference 类型、Q&A 字段及各类原生内容持久化。
- 发现并修正实际不一致：Q&A 分类正文入口 maxLength=80、抽屉 maxLength=160；现在统一为 80，测试验证抽屉限制。
- 完整桌面/手机运行 56/58 通过，剩余 2 项为问答设置抽屉标题定位；修正为当前“阅读范围与资料”后单独复验 2/2 通过。因此 58 项均已有通过证据（56+2），不是一次全绿运行。代码检查、TypeScript/生产构建通过。
- 旧编辑器回归缺口已关闭；此前失败记录保留作为经过，不再列为待办。R19 仍待真实账号/Safari/多标签竞态及线上验收，保持 [~]。未推送或部署。
- 下一子项 R01：搜索结果页移除重复文章侧栏，保留一套导航与 GitBook 搜索展示。

### R01 搜索页单侧栏（2026-09-12）

- 根因：外层 ReaderFrame 已提供业务侧栏，搜索分支又手动插入 TableOfContents，导致两套导航并排。
- 移除搜索分支内层文章目录，保留现有 GitBook SearchResults；独立结果容器适配桌面和手机宽度。服务端搜索、权限过滤和分析逻辑未修改。
- 本地验证：8 项搜索单元测试、24 项桌面/手机 Chromium 浏览器测试通过，lint 和生产构建通过；包含单侧栏、结果宽度、无横向溢出、中文搜索、分页及失败重试。
- 截图：output/verification/R01-search-desktop.png、R01-search-dark-desktop.png、R01-search-mobile.png、R01-search-dark-mobile.png。已查看桌面浅色及手机深色截图。这些使用真实页面框架与搜索组件的本地测试数据，不代表正式站或真实账号验收。
- R01 保持 [~]：本地修复完成，未推送/部署，正式站待验收。搜索类型文案和底部管理员提示分别保留给 R02、R03 处理。
- 下一子项 R02：搜索明确区分文章和独立 Q&A，修正数量及结果文案。

### R02 搜索类型与 Q&A 入口（2026-09-12）

- 搜索数量统一为“项结果”，零结果改为“没有找到相关结果”，不再把独立问答算作“篇文章”。总数继续使用服务端授权结果，不从当前分页推算分类数量。
- 每条结果以知识文章、OPS Internal、Reference 速查或 Q&A 问答标明所属模块；Q&A 使用问答图标及“查看答案”无障碍链接名称，沿用 GitBook 结果组件。
- 查明服务器导航树仍提供文章地址；仅对已授权 Q&A 搜索结果生成独立问答页链接和答案锚点，省去文章重定向。查询与权限条件未改变。
- 验证：10 项单元测试通过；24 项桌面/手机 Chromium 搜索与框架回归通过。首轮 8 项旧文案断言失败，更新预期后完整 24 项通过。查看了手机混合结果截图 output/verification/unified-search-mobile.png；使用本地样例，并非正式站。
- 本地修复完成，未推送/部署，真实账号及线上验收待办；R02 保持 [~]。下一项 R03：移除搜索底部重复后台入口，纠正二审配置提示位置及状态判断。

### R03 搜索重复入口与二审提醒（2026-09-12）

- 删除搜索结果底部重复后台/退出按钮，保留共享顶栏的账号与退出入口。移除阅读组件和访问页基于 initialAdmin 的“再安排管理员”提示，首次开通身份不再被用于推断当前人员配置。
- 二审提醒集中在后台提交二审面板，使用已有的服务端可用审核人名单；只有成功读完且确实为空时才显示。读取失败、加载中或仍有后续页，不宣称缺少管理员。无新增身份请求。
- 文案说明审核人不得是本次提交人、作者、编辑人，且账号须可用；引导核对成员与权限后重新读取。不是声称整个系统只有一位 Admin。有可用候选人后提醒消失。
- lint 与生产构建通过。同步审核旧测试到现有设置抽屉、输入标题删除和模态窗口操作；正式站、真实账号及 Safari 验收仍待完成。未推送/部署。
- 下一项 R04：分类入口与既定目录阅读结构统一。

- R03 最终回归：桌面/手机共 26 项全部通过（包含名单失败→空→可用、提交锁定、分页、删除保护和页面框架）。

### R04 分类入口直达阅读（2026-09-12）

- 分类路由在现有服务端授权目录中选取目标资料后，直接重定向至既有阅读地址；不再渲染“分类标题＋文章列表”的中间页。沿用现有目录顺序，旧 page 参数仍选择对应分页的第一篇资料。
- 重定向放在 try/catch 外，避免吞掉 Next 的重定向响应。登录准入、分类权限及文章目标再次授权均保留；空/不可访问分类不泄露分类名，服务故障与无内容状态分开。共享顶栏账号入口保留，移除分类页底部重复退出按钮。
- 8 项路由/分类单元检查通过（路由依赖隔离测试，不是真实账号访问）；16 项桌面/手机 Chromium 快捷入口、空状态和阅读框架回归通过。
- 本地修复，尚未推送或部署；真实账号及正式站分类跳转待验收，保持 [~]。下一项 R05：手机顶栏拥挤。

### R05 手机顶栏收纳（2026-09-12）

- 760px 及以下，阅读顶栏搜索改为 44px 按钮，展开后使用整行原搜索表单；支持自动聚焦、Esc 关闭并返回按钮、点击外部关闭及 Ctrl/Cmd+K 展开。首页保留既有主搜索区。
- 手机主题切换与管理员后台入口收进账号菜单；后台入口继续依据现有角色展示，服务端权限不变。桌面仍保留原搜索和外部主题/后台按钮。账号菜单可滚动，管理端小屏隐藏重复后台徽标。
- 26 项桌面/手机 Chromium 搜索与阅读框架测试通过，覆盖 320/390/760px 搜索、菜单、主题、键盘焦点及无横向溢出。已目视检查 output/verification/R05-mobile-menu.png 与 R05-search-320.png，均为本地样例，未代表真实账号或真机。lint 通过。
- 本地修复，未推送/部署，正式站、真实账号和 Safari/真机待验收，保持 [~]。下一项 R06：OPS 独立目录与阅读结构。

### R06 OPS 入口与独立阅读（2026-09-12）

- OPS 入口在已有授权查询成功后直接打开该页第一篇正式运营资料，不再要求从列表二次点击。旧分页参数仍按原规则工作，空/拒绝/故障状态保留，共享账号入口保留。
- 复用既有按 kind 过滤的 ReaderNavigation/GitBook 阅读组件，不另造阅读布局。服务器为 OPS 阅读返回 section 标识，用于桌面目录、手机抽屉标题及面包屑；面包屑返回 OPS。文章正文、附件、收藏及上一篇下一篇沿用既有阅读链路。
- 4 项 OPS 单元/路由检查通过；9 项临时数据库检查通过，包括 Support 拒绝、Ops 不获管理权限、下线后关闭访问，以及 OPS/普通知识目录互不混入。lint/build 通过。
- 本地修复，未推送/部署，真实账号与正式站验收待完成，保持 [~]。下一项 R07：Reference 多层入口及空状态。

### R07 Reference 入口与空状态（2026-09-12）

- 首次进入读取本页第一份已授权速查详情；指定 article 时仍读取指定资料，失败不会替换成其他内容。空列表不读取详情。
- 将长结果列表改为紧凑资料切换区并标出当前选择；表格直接展示。原文、完整 PDF 和管理员编辑保留在“其他操作”。空状态移除“选择一篇资料”的矛盾提示，底部重复退出移除，保留共享账号入口。
- 9 项路由/筛选/富文本单元检查通过，lint/build 通过。手机本地样例截图 R07-reference-mobile.png 已目视查看；表格允许容器内滚动，页面无横向溢出。
- 本地实现，未推送/部署，真实账号与正式站验收待办，保持 [~]。下一项 R08：后台补齐 Reference 管理入口。

### R08 后台 Reference 管理入口（2026-09-12）

- 后台侧栏新增 Reference 管理，指向已有 kind=reference 的列表；正确显示 Reference 管理标题及“新建速查资料”，新建使用既有 Reference 编辑类型。
- 修正知识文章侧栏的默认高亮条件，Reference 下只有对应导航高亮。复用原服务端按 kind 筛选与 Admin 检查，无新增权限或数据库变更。
- 3 项工作台规则检查、lint/build 通过。工作台旧工具菜单及个人范围下拉测试已同步到当前模块/范围标签。
- 本地修复，未推送/部署，真实账号和正式站待验收。下一项 R09：后台分类设置层级展示。


## R09 · 后台分类层级（本地完成）

- 分类列表改为嵌套树形结构，显示连接线、主分类／子分类、启用状态与实际访问范围；选中分类仍使用原编辑区。
- 长规则说明收进展开区；父分类和继承权限解释放在对应字段旁，保留原保存、冲突恢复与权限修改确认。
- 分类浏览器回归 16/16 通过，覆盖桌面、手机、十层分类、选中状态、保存冲突及权限确认；lint、build 与 diff 检查通过。
- 已查看手机十层分类截图：没有横向溢出，选中项和父子层级完整显示。
- 未推送、未部署；正式站及真实设备验收仍待完成。下一项 R10：统一设置确认弹窗。


## R10 · 设置确认弹窗（本地完成）

- 分类、字段、表单设置、导航、公告、功能开关、设置历史的原生 confirm 改用既有站内 confirmAction；保留原影响说明、服务端保存和重试规则。
- 分类和表单设置的链接跳转在等待用户确认前阻止默认行为，确认后再跳转；原生 beforeunload 提醒保留。
- 补齐共享弹窗 Tab／Shift+Tab 焦点循环，默认聚焦取消、Esc 取消及关闭后返回触发按钮；长弹窗限制视口高度并允许滚动。
- 最终 106/106 浏览器回归通过（桌面与手机），涵盖取消不写入、策略确认、输入保留、冲突、原请求重试、键盘及原生关闭提醒。lint、build、构建样式检查与 diff 检查通过；已查看桌面浅色和手机深色弹窗截图。
- 范围为设置模块；员工填写／处理表单与旧媒体编辑页的原生确认仍存在，本轮未宣称全站原生确认已清零。
- 未推送、未部署；正式站和真实设备仍待验收。下一项 R11：Q&A 重开重复读取。


## R11 · Q&A 同页答案复用（本地完成）

- 去除每次折叠都清空答案的行为；当前组件内存保存已授权答案，最多复用 30 秒，不写入 localStorage 或共享缓存。
- 展开状态到期重新请求；收起后不轮询，重新展开或锚点展开时检查有效期。列表传入正式版本，版本变动不沿用原答案。
- 失焦／隐藏页面清掉答案，回来及 pageshow 再核对；旧异步响应作废。失败、拒绝、错误问题 ID 和不合法版本不显示旧正文。
- 这不是实时撤权推送：持续停留在当前页面的权限／版本变动最多在下一次 30 秒复核时发现，不能声称瞬时同步；附件服务端检查保持不变。
- 最终 10 项桌面／手机专项通过，6 项 Q&A 请求／格式规则通过；lint、build、构建产物与 diff 检查通过。未执行全站回归。
- 检查还发现旧 qa.spec.ts 与其 fixture 仍对应早期列表 UI，本轮新增 qa-reuse.spec.ts 验证当前实际 QaAnswer，未将旧测试计为通过。
- 未推送、未部署；正式站真实账号权限和速度仍待验收。下一项 R12：取消收藏时仅更新当前条目。


## R12 · 取消收藏局部更新（本地完成）

- 收藏按钮成功确认后通知列表，去掉 window.location.reload；收到合法确认前不移除条目，未知结果仍保留原请求重试。
- 本地先移除已确认取消的条目并减少数量，再通过原受权 API 仅更新收藏列表和分页，补回被分页挤到下一页的条目；不重载整个网页。
- 列表刷新失败保留其他条目，说明取消已成功但最新列表未确认，暂时隐藏翻页以避免旧分页误导；拒绝访问时隐藏列表。并发更新使用代次避免旧列表响应覆盖新状态。
- 修正收藏按钮 effect 对临时 initial 对象的依赖，列表重新渲染不会意外作废其他条目的待确认请求。
- 16/16 桌面／手机浏览器检查通过，覆盖网页未刷新、失败保留与原请求重试、最后一页回落、列表读取失败；6 项客户端／输入／HTTP 规则通过。lint、build、构建检查与 diff 检查通过。
- 未推送、未部署；正式站真实账号验收仍待完成。下一项 R13：收藏与最近浏览按资料类型跳转。


## R13 · 个人资料入口按类型跳转（本地完成）

- 新增共用 contentPath：文章和 OPS 进入现有受权文章阅读页；Q&A 进入独立 question 链接及答案锚点；Reference 进入指定速查资料。
- 收藏列表、最近浏览列表和首页最近浏览接入共用规则；收藏失败的刷新入口也接受条目类型，避免问答／速查恢复时绕入普通文章。
- 不改变服务端权限过滤、已发布版本和附件检查。资料 ID 编码，不允许插入额外查询参数。
- 32/32 桌面／手机浏览器回归通过，含四种类型列表链接、收藏局部取消、浏览记录；2 项路径／参数规则通过。lint、build、构建产物与 diff 检查通过。
- 首页使用同一函数并通过构建，正式站完整点击链路和真实账号仍待验收；未推送、未部署。下一项 R14：文章反馈首次额外读取。


## R14 · 反馈首次读取（本地完成）

- 受权 reader 事务在功能开启时一并读取当前员工、当前正式版本反馈；已有反馈与确认没有反馈（null）都能直接初始化组件，省去首次独立 GET。未提供初始数据仍使用原读取及重试路径。
- 反馈组件按文章、正式版本及成员重新初始化，不使用跨成员共享缓存；保存仍检查 expectedVersion，冲突需重新读取。
- 12/12 桌面／手机反馈检查通过；临时数据库 50/50 授权回归通过，包括不同成员私有反馈和新正式版本无旧反馈。lint、build、构建检查与 diff 检查通过。
- 这是减少一次客户端往返，并非减少所有数据库读取；没有实测正式站耗时改善。未推送、未部署，真实账号及线上性能待验收。下一项 R15：编辑标题与文章目录提示。


## R15 · 标题与目录提示（本地完成）

- 文章与 OPS 正文上方新增默认收起的“如何生成文章右侧目录？”说明。名称采用当前 BlockNote 中文菜单：一级标题、二级标题、三级标题。
- 解释斜杠菜单和已有段落转标题、加粗不生成目录、文章名称与正文小标题区别，以及左侧分类通过添加分类管理。Q&A 与 Reference 不显示此文章目录说明。
- 不自动改写任何正文、标题类型或正式内容。
- 19 项正文／目录规则检查、2 项桌面／手机真实 BlockNote 编辑保存预览回归通过；lint 与最终 build、diff 检查通过。查看手机深色截图后补齐说明区边距。
- 未推送、未部署，正式站待验收。R16–R20 已完成本地整改，下一项 R21：PDF 屏幕顶部操作。


### R21 — PDF 共用顶栏与打印隔离（本地完成）

- PDF 不再绕过 ReaderFrame：共用搜索、账号、退出、主题与导航；没有共享布局时，EntryShell 也保留账号和搜索入口。
- 打印隐藏顶栏、导航、公告及操作按钮，释放外层布局宽度；正文和权限/版本复核不变。
- 截图复核发现深色账号菜单退出按钮白底低对比，已改用现有主题颜色。
- 验证：18 项桌面/手机浏览器检查通过，包含屏幕菜单、暗色、打印隐藏、下载失败重试及打印权限复核；lint、最终构建、diff check 通过。
- 账号采用隔离模拟身份，只验证入口可见和可用状态，未声称真实 Clerk 退出完成。真实身份、Safari/真机及部署后打印仍待验收。未推送、未部署。
- 下一项 R22：日常编辑读取所需版本，历史按需加载。


### R22 — 编辑历史按需读取（本地完成）

- getEditor/saveEditor 使用专用精简读取：最多当前、已发布、最大编号三个版本及最后一条审计记录。保存编号仍从最大版本递增，重复请求使用最后操作校验，文档锁和权限检查保留。
- 不删除或截断数据库历史。历史页原有分页/单版本读取保持不变；通用管理、审核和发布仍用完整读取，避免削弱审批证据判断。本次不宣称这些路径也已优化。
- 新增临时数据库样例：36 个真实版本，修改前读取量断言失败；修改后打开、保存、重试均为有界读取。保存后37版本/41事件完整，旧发布版本继续可读，冲突和非管理员拒绝正常。
- 51 项授权/编辑测试及19项历史/双管理员流程检查通过，共70项。lint、最终构建和diff check通过。全部使用隔离临时数据库，没有修改正式资料。
- 未推送、未部署。正式站耗时、长期大规模历史数据性能仍待验收；下一项 R23：保存锁范围。


### R23 — 保存共享配置锁（本地完成）

- 根因：应用 insertRevision 和数据库字段/分类触发器都使用独占配置锁，不同文章也排队。
- 普通保存两处均改为 shared advisory transaction lock；字段/分类设置写入、设置恢复等仍用独占锁。文档锁、成员检查、字段和分类校验均保留。
- 新增0029_shared_revision_config_locks迁移，只替换两项触发器函数的锁模式。历史迁移和正式内容不改动。
- 并发验证：A保存带分类文章并保持事务，B不同文章可完成；配置修改在A结束前仍被锁保护，结束后停用成功，新选停用分类被拒。修改前同一测试会超时。
- 74项专项通过；完整数据库391项中389项首轮通过，旧迁移计数两项更新28→29后，core整组13项复验通过。lint、最终构建及diff check通过。测试仅使用临时数据库。
- 发布顺序：先迁移0029，再发布共享锁应用代码，避免新应用遇到旧独占触发器。旧应用搭配新触发器仍保持原有串行保护。
- 未执行正式库迁移，未推送、未部署；正式并发耗时待测。下一项R24：媒体身份检查测量。


### R24 — 媒体身份链路测量（本地仪表完成，线上诊断待办）

- 不删除授权、交付前复核或引入身份TTL缓存。普通资产与PDF导出新增请求级阶段汇总，复用JUYU_PERFORMANCE_LOGGING=true开关，默认关闭。
- AsyncLocalStorage隔离并发请求；只记录固定route/stage、次数、耗时、失败次数和HTTP状态。没有账号、文件名、路径、SQL、令牌或异常正文。
- 分开观察identity.verify、Clerk/Slack真实调用、asset.authorize/recheck、PDF snapshot/asset/recheck/render、storage.headers及PDF storage.body。
- 本地成功样例：普通附件2次授权；单封面PDF4次授权（文章和封面各前后一次）。这些是授权回调计数，不是线上Slack调用量。成功完整PDF有N个不同媒体引用时预期2+2N次授权；check=1预检查不含生成后复核。
- 14项日志/存储专项及58项隔离数据库授权/权限矩阵通过，共72项；lint、构建、diff check通过。拒绝/撤权、并发隔离、关闭日志无输出已覆盖。
- 未推送、未部署、未开启正式日志。真实服务耗时和重复供应商调用仍无法确认，不能宣称提速或R24全面关闭。
- 下一阶段：先完成待部署迁移与代码发布，再短期开启汇总日志，比较附件、带多图文章、PDF和视频Range请求。按慢阶段选择优化，仍保留身份撤销和交付前保护。


### 发布进度 — 2026-09-12

- 用户授权执行迁移、推送和部署。正式项目zscxaqjqjoouiolkoxbi原有28个迁移；0029_shared_revision_config_locks已成功执行，两个函数共享锁读回验证通过。未修改文章内容；原函数定义留在本机忽略目录output/verification。
- 发布前387项规则测试通过，最近一次完整构建通过。GitHub main与本地HEAD一致，尚未提交本批代码。
- 自动审批拒绝整批提交并推送main，理由为广范围默认分支正式发布未获其认可的明确授权。该命令没有执行；等待用户明确批准本次24项T063整体发布。当前生产仍使用旧应用，其独占锁与新迁移兼容，只是并发改善尚未上线。

### 2026-09-13 实际上线与缺口补验

- 7aebccf 已推送部署，后续 da7fb6a 已正式部署；早期“未提交/未部署”仅为当时记录，不代表当前状态。0029 与0030正式迁移已执行。
- 本轮执行记录见 docs/verification/2026-09-13-t063-gap-closure.md。全部 [~] 不自动改为 [x]：剩余真实身份、设备、数据与性能验收仍须证据。
