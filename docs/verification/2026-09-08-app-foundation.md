# T007 · 网站工程基础验收

日期：2026-09-08。路径：Juyu V2。

## 已交付

React/Next.js App Router 网站，Tailwind 共用样式；总入口、员工和管理员未连接页；资料/后台路径重定向；404 返回及基础错误重试页；进程健康接口和独立就绪状态接口；空环境变量模板与配置格式检查。

使用 Next.js 16.3.1、React 19.2.8、Tailwind 4.2.1、TypeScript 5.9.3。版本与用户提供 site 的已安装包保持兼容，本次依赖独立安装，没有复制其 node_modules 或环境变量。安装进程首次使用了主机默认 Node 26 并提示工程版本不符，所有验收已明确使用 Node 24.19.0，`.nvmrc` 固定该版本。

## 自动验收

- 51 项单元测试通过：原有 43 项领域规则，加 8 项配置/就绪边界测试。
- 8 项 Playwright 测试通过：桌面 1440×1000、手机 viewport 390×844 各 4 项。
- `npm run build` 通过；`npm run lint` 无错误无警告；`npm run typecheck` 通过。
- 先执行缺失实现时的测试：配置测试 7 项失败；最小应用上的 4 项桌面用例失败。补实现后通过。

浏览器验证真实 production server：入口键盘跳转、员工未连接状态、Admin 路径忽略 role/demo 参数、不可用登录按钮、切换入口、404 返回、health 200 与 readiness 503/no-store；入口宽度没有横向溢出，员工导航未出现 pageerror。

## 视觉检查

截图保存在 `output/verification/t007-*.png`，为运行页面截图，不是生成设计图。桌面入口和手机管理员页已人工查看：中文、按钮、未连接说明和页脚可读。本轮使用 Chromium 的手机尺寸模拟，没有宣称真实手机或 Safari 验收。

## 独立审查

只读代码审查未发现阻止 T007 完成的实质问题；核对了未连接边界、配置输出、路由及现有领域逻辑的使用关系。审查者没有另行执行浏览器测试，测试结果来自本轮实际运行。

## 边界

没有 Clerk 登录/退出、Slack 验证、Supabase 事务/持久化、实际角色会话、BlockNote、GitBook 阅读 UI 或 Tasks 工作台。公开页面不包含真实资料。readiness 固定表示认证和数据库尚未集成，填入配置本身不会开启登录。当前 UI 是自编入口，不记为第三方 UI 已移植。没有部署到公网。

下一项：T008 核心数据库结构与发布/审核事务。
