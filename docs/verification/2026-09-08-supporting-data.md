# T009 · 辅助数据表本地验收

日期：2026-09-08。工程：/Users/tony/Documents/ChatGPT/Juyu V2。仅使用自动创建、回环地址监听的临时 PostgreSQL；未连接真实 Clerk、Supabase 或旧站数据库。

## 交付内容

追加 migrations/0002_supporting_data.sql，新增 14 张表：categories、revision_categories、assets、revision_assets、favorites、recent_views、feedback、search_queries、search_results、analytics_events、settings、setting_versions、notifications、notification_receipts。

公司范围明确为单公司独立部署；表关联到稳定成员/资料/版本 ID。原 0001 SQL 不改写。迁移执行器按版本追加，旧版测试的迁移数量随之从 1 更新为 2；校验和故障测试现在只改 0001 的记录。

## 实际检查结果

- npm run test:db：24/24 通过，0 跳过，0 失败；其中 T008 13 项、T009 新增 11 项。最终执行约 2 秒。
- npm test：51/51 通过。
- npm run typecheck：退出码 0。
- npm run lint：退出码 0。
- 本地 /sign-in：200；/api/health：200；/api/readiness：503，真实服务未接入状态保持正确。
- 本次没有改变 Next 页面、样式、依赖或运行接口，不重复构建和浏览器交互验收；不将上轮结果写成本轮新测试。

## 已验证的关联

- 所有 14 张表成功创建；改分类名称保留子目录和版本归属；孤儿版本引用/分类删除受外键限制。
- 附件无法跨资料引用；INSERT 时拒绝公开桶/URL 对象键，UPDATE 时拒绝改变对象身份；负文件大小拒绝。
- 收藏/最近浏览按成员和文章唯一；不存在的成员或版本被拒绝。
- 帮助反馈每成员每文章版本一条，可更新，同篇新版本不会混入旧版本反馈。
- 搜索点击绑定该员工查询的实际结果；其他员工、另一篇文章、缺少查询参数或重复事件 ID 均拒绝。
- 配置当前版本不能引用别的设置；配置历史不可修改或删除，保留修改人；配置仅接受 JSON 对象。
- 公告目标外键有效；过期时间不能早于开始；员工已读和关闭状态独立。
- RLS 测试自行准备非空数据，逐表确认所有者可见后切换无策略角色，14 表均不可读/删除，非法插入也被拒绝；不以空表充当权限通过。
- 重复迁移保留 14 表行数和核心文章聚合。
- 另建临时旧版数据库，写入真实领域文章后升级：只应用 0002，原文章聚合完全不变，再次执行为空。

## 开发和审查记录

先写 14 表存在断言，实际运行失败为 0 != 14，再实施迁移。首轮完整测试暴露测试参数同时被推断为 UUID/text 的问题；拆分参数后通过，权限测试同时改为独立准备数据，避免依赖其他测试顺序。

独立只读审查未发现 T009 阻断性问题，提出两项测试改进，已完成：非法附件 INSERT 断言 23514（避免只验证 UPDATE 触发器），迁移重放增加核心聚合读回。最终 24/24 再次通过。

## 完成边界

T009 数据结构及本地验收完成。没有收藏按钮、分析页面、配置编辑器或新功能公告 UI；没有实际文件上传下载；没有生产登录和权限验收。分类完整防循环、版本元数据冻结写入、配置语义校验、事件采集/清理、公告目标权限均由已列出的后续任务实现。

下一项 T010：数据库授权与服务端统一授权入口。完整表说明和后续要求见 src/server/database/SUPPORTING_DATA.md。
