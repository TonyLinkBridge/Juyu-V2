# 核心规则本地验收

日期：2026-09-08。任务：T004、T005、T006。

## 结果

- `npm test`：43 项通过，0 失败，0 跳过。
- `npm run typecheck`：通过。
- 环境：Node 24.19.0，TypeScript 5.9.3，Node 类型 22.19.19。
- 开发依赖安装成功；首次在受限网络中遇到 DNS 解析失败，授权网络重试后成功。

## 验证的行为

18 项访问测试：三个角色和三种可见范围的矩阵；未知/未验证身份；归档/回收站/未发布；新旧版本隔离；搜索不返回受限内容；附件必须关联当前正式版本；读取结果不能修改存储快照。

25 项流程测试：草稿创建、内容校验、非 Admin 拒绝；A 提交给 B；作者/提交人/编辑人不能自审；指定二审批准和退回；撤回、重分配、意见和重提；批准→等待发布→发布；新稿保留旧正式版；改动使批准失效；陈旧操作冲突；不可通过普通操作重新发布归档/回收站资料；历史记录完整。

先写测试和函数签名，再补实现：访问测试首次 11 项失败；流程测试首次 24 项失败；实现后全部通过。最终检查同时执行完整测试和类型检查。

## 未覆盖、未完成

真实 Clerk 登录/退出、邮箱和 Slack 校验、首位账号初始化、Supabase 事务/RLS、数据库持久化、私有文件接口、真正搜索/PDF 导出、Liveblocks、多用户网络并发、前后台页面、真机与部署均未验收。不能从这里的本地通过推断这些功能已完成。

UI 目前仍只有视觉稿。整个资料库尚未完成；剩余任务见 `TASKS.md`。

## 独立审查

代码审查未发现可复现的实质逻辑缺陷。按审查建议补充作者、编辑者、提交者分属不同管理员时的换二审拒绝用例；补充后完整测试 43/43 通过，类型检查通过。
