# 首页直接进入员工登录 · 验收

2026-09-08。用户确认取消登录前的双入口选择。

- `/` 直接重定向到 `/sign-in`；不再展示员工/管理员两张选择卡片。
- 员工登录页不含后台入口或后台切换链接，也没有返回自身的多余入口链接。
- `/admin` 仍进入独立的 `/admin/sign-in`；该页可以切回员工登录。
- 404 返回首页会继续进入员工登录，避免回到旧选择页。
- 新浏览器测试在旧构建上先失败，更新后桌面和手机尺寸 8/8 全部通过。
- lint、正式构建及构建内 TypeScript 检查通过。
- 手机员工登录运行截图已查看，文字、状态提示及禁用按钮可读。截图使用 `output/verification/entry-flow-*.png`，保留先前 T007 截图作为历史。

真实 Clerk 登录未接入。有效会话直接进入资料库，以及仅 Admin 登录后可见后台入口，仍由 T012/T014 实现。本次不声称完成登录后的角色分流。
