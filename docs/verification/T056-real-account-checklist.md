# T056 真实账号验收表（待执行）

当前状态：**未执行，不能签收为真实通过。** 本地连续演练已另有记录；它没有创建实际Clerk会话，也没有连接Supabase测试项目。

## 准备

填写测试站地址、Clerk测试实例、Supabase测试项目、公司邮箱域名及Slack Workspace允许列表。密钥只进入安全配置，不写入此表。使用通过公司准入的管理员A、管理员B、客服Support和运营Ops；增加管理员C以检查未指定管理员不能审批。

A、B、C及员工分别使用独立浏览器配置文件，避免同一浏览器的会话切换互相影响。确认当前角色来自服务端验证，不以界面或手工请求参数代替角色。只在测试项目中新建标为“T056 验收样例”的资料和附件。

记录：测试站______；Clerk实例标识______；Supabase项目标识______；执行日期______；执行人______；文章编号______。

## 连续操作

每行保存实际状态、文章版本和必要的截图/请求状态码；错误操作还要核对历史记录没有新增成功动作。账号标识可用A/B/C代号，不保存会话Cookie或令牌。

| 步骤 | 操作人及操作 | 应看到的结果 | 真实结果/证据 |
| --- | --- | --- | --- |
| 1 | A 从 `/admin/sign-in` 登录，员工从 `/sign-in` 登录 | A能进入后台；员工没有后台权限；非公司或错误Workspace账号不得读资料 | 待验收 |
| 2 | A 新建文章，保存正文并上传、选择封面 | 草稿已保存；员工正文、搜索、封面链接和PDF均不可读 | 待验收 |
| 3 | A 提交时选择B | 等待审核；B“等我审核”出现此文，A/C不能决定；审核稿锁定 | 待验收 |
| 4 | A和C打开同一审核页，并尝试提交批准请求 | 不能代替B；后台状态及历史不变 | 待验收 |
| 5 | B先不填原因尝试退回，再填写原因确认退回 | 空原因被拒；填写后进入需要修改；A能看到对应版本的原因 | 待验收 |
| 6 | A未修改先尝试重提，再修改保存后提交B | 原稿直接重提被拒；新稿能再次进入等待审核 | 待验收 |
| 7 | B批准当前版本 | 进入已经批准；员工仍不可读；批准动作只记一次 | 待验收 |
| 8 | A安排发布，再执行发布确认 | 先等待发布，之后已经发布；员工能读正式正文、封面及PDF，搜索能找到 | 待验收 |
| 9 | A修改已发布文章，保存另一版本 | 后台出现新草稿；员工正文/搜索/PDF继续显示旧正式版，新稿不泄露 | 待验收 |
| 10 | A把新稿提交B，B批准，A安排并发布 | 审核各阶段旧版继续可读；发布成功后才切换为新版，旧页面保存不得覆盖新状态 | 待验收 |
| 11 | A删除测试文章 | 进入回收站；员工正文、搜索、图片直链和PDF都失去访问；旧下载地址也重新检查 | 待验收 |
| 12 | A在回收站选择“恢复为未发布草稿” | 恢复到草稿，员工仍不可读；历史批准不能直接再次发布 | 待验收 |
| 13 | A重新提交B，B批准，A安排并发布 | 再次成为正式资料；历史保留删除、恢复和新审核轮次 | 待验收 |
| 14 | A在历史中选择较早版本恢复 | 较早内容复制成新草稿；当前正式版仍可读；再经B批准及发布才替换 | 待验收 |
| 15 | A/B检查完整历史 | 提交者是A，批准/退回人是B；退回原因、版本、时间、删除/恢复和发布均正确；重试没有重复成功记录 | 待验收 |
| 16 | 员工退出后访问先前文章/图片/PDF；切换为受限账号再访问 | 新请求不得沿用原账号权限；检查真实浏览器返回、共享代理/CDN及Supabase私有直链 | 待验收 |

## 签收规则

实际未执行、超时或无法确认，一律保留“待验收/无法确认”，不写“通过”。批准不等于发布，恢复不等于发布，进程健康不等于业务已接通。任何真实环境差异记录文章编号、步骤、可复现现象和脱敏证据，修复后重验受影响步骤。

完成状态：待验收。阻塞项：Clerk/Supabase测试配置、公司准入允许列表和真实测试账号。
