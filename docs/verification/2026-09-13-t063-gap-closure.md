# T063 验收缺口补修 · 2026-09-13

## 本轮范围

用户批准按复核清单继续。代码、隔离测试、正式站行为分别记账，不把 24 项一并标完成。

- R10：员工填写/处理表单及旧媒体重新载入改用站内确认。表单接入共享离开保护；未知提交继续锁定原操作。媒体确认后不再二次弹出系统提醒。源码中 window.confirm 已清零；原生 beforeunload 仍保留。
- R18：无 Navigation API 时使用 capture-phase popstate，先恢复当前位置，避免路由卸载输入后才询问。已跟踪的前进/后退保留历史；进入组件前的未知历史位置保守恢复当前页，确认后完整导航，可能丢弃该位置之后的前进分支。页内锚点不拦截。不能声称已实测 Safari 真机。
- 搜索回归：同步已发布标签，额外断言不显示内部修订为正式版本。
- R19：本轮完整 editor.spec 58/58 通过，包含恢复、版本冲突、显式保存；真实 Clerk 退出和多标签竞态仍未验收。
- R16：正式账号在内置浏览器点击下载，页面返回“PDF 已生成，已交给浏览器下载”。Vercel 同次 /pdf HTTP 200，服务端计时7008ms。未取得浏览器落盘文件作逐页核验，不把有图/多页/撤权场景视为已验。
- R24：正式日志已启用（本轮未修改该配置），与此前“未开启”的旧记录不同。此次无图 PDF：identity.verify 2次/3171ms，clerk.user 4次/1152ms，slack.userinfo 2次/537ms，pdf.render 3391ms。阶段含嵌套，不能把所有耗时相加。
- R24 修复：单次 currentVerifiedMember 内复用同一用户读取；每次交付后检查创建独立作用域，Slack前后复核保留，不跨请求缓存。正式优化后数据待部署复验。

## 尚未关闭

真实 Support/Ops 权限、Safari/真机、多标签草稿恢复、复杂正式 Reference、带图/多页 PDF 内容及交付期间撤权、真实多人保存负载、附件与视频 Range 的完整性能样本，不能由上述测试代替。

## 验证经过

先复现默认确认框与无 Navigation API 后退失败，再通过修复；整组124项通过。随后新增并复现页内锚点/重复系统确认两项边界，修复后重新运行整组。最初一次大组有测试依赖缺失及构建与测试争用产物错误，未计为通过；补依赖并顺序复验。

- 最终：128/128 表单、媒体、审核、发布及搜索浏览器测试；58/58 编辑器测试；393/393 应用规则；正式构建（含TypeScript及资源检查）、lint、diff检查通过。
- 发布状态：提交与推送 main 的命令被自动审批拒绝，未执行。新改动仍在本地，优化后的正式请求计数未验证，禁止标为已上线。

## 正式部署复验

- 用户明确批准后，1c94e0e 已推送 main；Vercel 回报 Deployment has completed。
- 同一正式文章下载成功，界面返回“PDF 已生成，已交给浏览器下载”，日志 HTTP200。
- 新样本5875ms；clerk.user由4次降为2次（585ms），identity.verify仍2次，slack.userinfo仍2次，pdf.recheck仍1次。前后复核保留。
- 旧样本7008ms，新样本5875ms；只有各一次样本，不能当作稳定提速比例或全部页面性能结论。
- 本节是部署后本地验收记录，尚未另行推送。此前剩余真实设备、身份和复杂数据场景继续保持待验。
