# T017 左侧文章目录：本地验收

日期：2026-09-08

## 已交付

实际适配用户指定的 GitBook 目录源码，接入 Juyu V2 员工页。六个来源文件记录于 `docs/sources/T017-navigation.json`，并更新主来源清单。styles.ts 原样复制；列表、文档链接、侧栏和滚动定位提取相应分支并替换供应商数据与主题依赖。没有宣称完整 GitBook 功能已移植。

服务器从当前可信身份发起受限只读事务，仅返回 can_read_revision 允许的正式标题、ID 和固定站内链接。Admin 在阅读端也只看正式内容。新草稿不更改目录正式标题，归档或分类停用会使文章从目录消失。客户端不接收正文、后台状态或未授权标题。

目录点击通过员工页重新核对权限；按精确 ID 选择当前项，提供 data-active 和 aria-current。不存在和无权文章使用相同不可用提示，不显示传入的未知标题或 ID。目录为空与服务失败分别显示，失败可重新加载。

电脑使用左侧独立滚动目录，手机暂用可滚动的内联目录。刷新靠后文章时，直接适配 GitBook 当前项定位效果。T018 分组/子目录、T019 正文、T021 手机抽屉尚未交付。

## 本地验证

| 验证 | 结果 |
| --- | --- |
| 全部单元测试 | 110/110 |
| 临时 PostgreSQL 数据库测试 | 68/68 |
| 电脑及手机浏览器测试 | 44/44 |
| 生产构建 | 通过 |
| 类型检查、lint | 通过，lint 无告警 |

浏览器 44 项包括 26 项真实未配置网站检查，以及开通、成员、目录各 6 项隔离组件检查。目录 fixture 使用实际组件和本次生产构建的 CSS；示例文章仅在浏览器拦截的测试响应中存在，页面明确标识“本地目录测试 · 示例内容”。没有加入生产示例文章或认证旁路。

新增数据库测试覆盖 Support/Ops/Admin 可见范围、未登录拒绝、Admin 草稿过滤、旧正式标题保持、归档及分类祖先限制。链接编码与精确选中在单元测试验证。浏览器验证点击、刷新、返回、键盘、长标题、无横向溢出、空/错误状态及刷新末项自动可见。

已人工查看生产 CSS 下的两张截图：目录与标题可读，当前项标识清楚。手机内联目录为本任务过渡布局，不代表 T021 已完成或完成真机验收。

- `output/verification/navigation-desktop.png`
- `output/verification/navigation-mobile.png`

独立复核确认源码来源和权限查询未发现其他 P1/P2。复核发现的长目录当前项定位遗漏已补齐：修复前新增测试失败，修复后电脑/手机通过，复核确认已修复。

## 验收边界

Clerk/Supabase 测试项目和公司允许列表仍待配置。本次未修改真实成员、未连接云端资料、未部署。浏览器 fixture 和临时数据库测试不能代替真实账号联合验收；实际未配置网站继续显示登录准备页。T017 标记本地完成、真实服务待验收。

下一项：T018 分组、子目录与排序。
