# T060 本地恢复演练记录

日期：2026-09-10。源代码基础：42309f0 + 本次未提交 T060 变更。

## 实际执行

命令：`JUYU_PG_BIN=/opt/homebrew/opt/libpq/bin npm run test:recovery`。

运行环境：Node 24.19.0、embedded-postgres 17.10.0-beta.17、pg_dump/pg_restore 18.6。两套内部生成的临时 PostgreSQL 集群，两个本地模拟 Storage 服务，另外一个空 Storage 目标用于损坏验证；没有读取生产 .env，没有连接真实 Supabase。

- 36 张 juyu 表：逐表排序后的完整行内容 SHA-256 和行数一致，序列值一致。
- 26 份迁移 SQL：归档并核对 schema_migrations 的 SHA-256；恢复后 migrate 返回空列表。
- 3 篇资料：普通正式文章带待二审修改、OPS 正式文章、未发布草稿；审核与审计数据随表完整恢复。
- 1 个实际样例附件：数据库中关联正式文章版本，原字节经过 backupFiles / restoreFiles 恢复并比对。
- 故意写入的旧 request_contexts 数据没有进入恢复目标。
- 恢复后用独立受限数据库登录账号：Support 只看到普通文章旧正式标题及其附件；Ops 可看到普通和 OPS 文章；匿名应用身份被拒绝；直接匿名访问模拟私有文件返回 401。
- 临时 schema 变更遇到故意制造的 SQL 失败后回滚，探针表不存在、其余表内容不变。
- 媒体备份字节损坏后，restoreFiles 在写入空目标前拒绝；目标对象仍不存在。

最终执行：1 项综合恢复测试通过，0 失败。综合案例耗时约 1.26 秒（极小本地样例，不能作为生产 RTO）。最终样例 dump SHA-256：`31b71c0b4ae48f89af88134e35b9e76d4c9b6db69522f113cd43ec5bd1cb02ef`。

lint 和 TypeScript 检查通过，git diff --check 通过。未更改生产运行代码，因此本次未重复完整应用构建。演练临时实例和样例备份在结束时清理；该哈希用于记录本次执行，不对应长期保管的生产备份。

## 未完成的验收

线上健康接口仅为 web_process_only；本次未做真实 Clerk / Storage / PDF 健康验收。未执行云端隔离恢复、Vercel 正式回退、自动备份排程、加密异地保管或保留周期配置。数据库失败事务回滚不等于应用版本回退。T060 保持部分完成。

下一步使用独立 Supabase 恢复项目和空私有 bucket 验证真实备份恢复，再以已验收且兼容当前数据库的部署作为 Vercel 回退目标。正式切换前必须明确目标部署、配置差异及恢复期间新增数据的处理方式。

## 真实云端验收追加（同日）

用户已创建独立恢复项目 `juyu-recovery-t060`（`bgrvonvrytlaufityutq`，Tokyo），并将独立密码和服务端存储密钥保存在本地环境配置中。正式源项目为 `zscxaqjqjoouiolkoxbi`（Singapore）。没有修改生产数据库内容。

### 数据库

已实际执行 `scripts/t060-cloud-database.ts`。脚本仅允许以上两个项目，目标已有 juyu schema 时拒绝覆盖，使用严格 TLS。导出期间通过短暂 SHARE 锁与 PostgreSQL 导出快照固定源资料，导出结束释放锁，然后恢复到新项目。恢复过程不复制生产登录密码。

源共有 37 张表，其中 36 张持久数据表逐表完整行内容、行数及序列比对一致；request_contexts 表结构保留，旧临时授权数据排除。1 篇真实资料、26 份迁移 SQL/校验值、38 条权限策略恢复成功。恢复与比对约 83.8 秒，仅是当前小数据量及跨区域条件下的单次测量。源附件清单为空。

安全结果摘要保存在本地被 Git 忽略的 `output/verification/t060-cloud-database.json`。临时原始 dump 和迁移副本在验证后清理；这不构成已配置的长期异地备份。

### 媒体

已实际执行 `scripts/t060-cloud-media.ts`，使用新项目真实 Storage 的私有 juyu-private bucket。新建一次性 45,056 字节样例，执行备份、拒绝覆盖、删除该样例、恢复、逐字节哈希比对、匿名访问拒绝、损坏备份拒绝以及最终清理。恢复加读取比对约 1.73 秒，匿名 public URL 返回 HTTP 400 且无法取得文件。真实生产附件为空，所以此项是云端样例文件演练，不是生产附件正向读取验收。

云端实测暴露并修复初始化兼容问题：Storage 可用 HTTP 400 包装 `NoSuchBucket` / `statusCode: "404"`。新增回归测试先失败、修复后通过；只接受明确缺失代码，不把 AccessDenied 等其他错误当成可创建条件。参考 https://supabase.com/docs/guides/storage/debugging/error-codes 。

### 部署回退：尚未执行

当前生产：`42309f0`，deployment `38omvswz5DtNNdzgX6hT9MfwVn5t`。
回退候选：`03d9746`，deployment `D56XfRF9ytRCXTRKGceTnLh2mzT9`。
两版本 migrations 目录无差异。正式域名 /api/health 返回 200、web_process_only。独立部署健康地址返回认证跳转，浏览器进一步访问旧部署被 ERR_BLOCKED_BY_CLIENT 阻止，因此未验证旧部署健康。

已准备 Vercel Confirm Rollback 确认页，但点击被自动审批拒绝：旧部署健康未验证，且要求明确批准此次生产域名变更。未通过其他工具绕过拒绝，正式域名未回退。拟在获得明确批准后执行 `42309f0 → 03d9746`，验证后 Undo Rollback 回到 `42309f0`，同时恢复自动发布。参考 https://vercel.com/docs/instant-rollback 。

### 本轮检查与状态

343 项规则测试通过（含新增存储回归），lint、TypeScript、Next.js build 通过。现有 T059 文档未改动。本轮未推送 GitHub、未发布兼容修复。

T060 保持部分完成，具体剩余为实际 Vercel 回退及回退后的健康验收。自动排程、加密异地保管与保留周期仍未配置；这些持续运维能力不能从本次恢复演练推断已经具备。Clerk 真实角色、二审完整流程和 PDF 等 T059 验收也不在本次自动完成。

## 最终生产回退验收（2026-09-10 21:01–21:07 MYT）

用户在得知旧部署预检受阻及生产切换风险后明确回复“执行”，本次自动审批允许操作。

1. 在 Vercel Confirm Rollback 执行 `42309f0` → `03d9746`。界面显示正式域名分配成功，刷新 Overview 后显示 Rolled back、旧部署 `D56XfRF9ytRCXTRKGceTnLh2mzT9`。
2. 21:01:21–21:01:25 MYT，正式域名 `/api/health` 返回 200 / `web_process_only`；`/sign-in` 返回 200 且包含 Clerk；匿名 `/admin` 返回流式页面内的 NEXT_REDIRECT `/admin/sign-in`，未观察到服务端异常页。此处 HTTP 200 不是后台授权成功。
3. 立即开始恢复原版本，在 `38omvswz5DtNNdzgX6hT9MfwVn5t` 的 Promote 确认页核对 `42309f0` 和正式域名，界面明确说明将撤销 Instant Rollback，随后执行。
4. 21:04:49–21:04:54 MYT 重复健康、登录页与匿名后台保护检查，结果均通过。
5. Production 环境设置实际显示 Auto-Assign Custom Production Domains = 1（Save 禁用，没有待保存修改）。Overview 最终显示 `42309f0` / Ready / `juyu-helpcentre.vercel.app`，并重新显示 main 分支推送更新生产的说明。

结果：T060 的恢复与回退演练验收完成。正式网站已回到原当前版本，自动发布恢复。本次没有修改数据库内容、环境变量或推送新代码；没有把恢复项目接入正式网站。

验收范围：进程健康、登录页可响应、匿名后台保护与实际部署切换。没有在回退窗口执行真实管理员内容写入、Clerk 多角色业务流程或 PDF 验收；这些仍按 T059 记录。长期加密备份排程及保留周期尚未配置，不能把一次恢复演练称为持续自动备份已就绪。

前文“回退尚未执行／T060 部分完成”为本次批准前的历史状态，以本节最终验收为准。
