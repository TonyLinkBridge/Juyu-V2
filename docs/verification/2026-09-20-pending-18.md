# 9 月 14–20 日待处理记录：修复与验收

实际仓库：`/Users/tony/Documents/ChatGPT/Juyu V2`。开始时分支为 `main`，工作区干净。本地验收阶段没有部署、推送或修改真实文章。用户要求不用 subagent 后已立即停止；后续修复与验证在本任务内执行。

## 验证范围

- 首次有效基线：401 项单元测试，397 通过、4 失败；lint 有 2 个真实错误。
- 最终运行环境：项目指定的 Node 24.19.0。系统默认 Node 26 的中间结果不作为最终验收依据。
- `npm run verify:build`：类型、lint、全部单元测试、生产构建及 CSS/浏览器资源追踪检查。
- 数据库测试：临时 PostgreSQL，覆盖 authorization、qa、recent、reference、favorites；不会连接真实业务数据库。
- `npm run test:critical`：桌面及手机的登录边界、编辑器、收藏、OPS、Reference、Q&A、后台筛选。交互测试使用隔离样例及明确模拟的身份；不等同真实 Clerk 账号验收。
- 最终结果：412/412 单元测试、97/97 数据库测试；164/164 桌面与手机关键页面整套测试。最后补充“按钮已卸载时退出清缓存”后，全部收藏相关 26/26 复测通过，其中包含新增的桌面/手机边界用例；其余 24 项与整套测试重叠，不重复累计。
- 最后补丁后的类型检查、lint、生产构建及构建资源检查均退出码 0；`git diff --check` 无错误。
- 原始验收日志保存在 `output/verification/pending-18/`（本地生成文件，不提交仓库）。
- 本记录为提交前的本地验收快照；后续提交与推送状态以 Git 记录为准，部署和线上验收需另行确认。

## 逐项记录

| 编号 | 确认的事实 | 修复 | 验证与边界 |
|---|---|---|---|
| 1 OPS 入口 | `redirect` 的控制流异常被读取失败的 catch 吞掉；旧桩还缺少 `firstOpsId`。 | 读取完成后在 catch 外跳转；仅无首篇时读取空集合。 | 路由测试验证有文章跳转、空集合、无权限、服务失败；OPS 组件浏览器测试验证最终可见状态。 |
| 2 提示类型 | 提示用是否包含“无法”推断失败；非法文件及“上传未确认成功”被当作成功。 | 上传、格式、复制、草稿保存显式传递 success/error；草稿成功是否弹出也用独立字段。 | 非法文件浏览器测试先失败、后通过；成功上传与自动保存分别断言。 |
| 3 双标签冲突 | 乐观版本检查和输入备份已存在，但主按钮统一显示重试。 | 冲突、字段冲突、审核/停用状态变化改为“查看最新版本并对照”；网络不确定结果继续保留原请求供重试。 | 真实编辑器测试验证对照不写入、保留输入、确认载入后保留备份、再保存使用新 sequence、服务器再次变化仍冲突。没有把所有历史 HTTP 400 归因于双标签。 |
| 4 收藏缓存 | 原缓存仅以文章/修订为键；模拟同页换会话可复现旧状态残留，尚无线上跨账号事件证据。 | 以 userId/sessionId 隔离，30 秒有效期、最多 50 条；身份变化重挂组件，服务端初始值需匹配 viewerId；退出清空并使迟到回包失效；即使已离开文章、按钮已卸载，缓存清理监听仍有效；回到文章复核权限。 | 会话切换、退出、迟到回包、无重复快速写入、初始服务端值零重复读取、权限撤销测试；缓存容量/有效期测试。真实账号切换仍需上线环境验收。 |
| 5 OPS/Reference 测试 | 4 项失败含过时接口桩；不能据此一概认定生产功能失败。 | 更新 `firstOpsId`、`referencePage` 契约，增加 Reference 权限/服务失败区分。 | 单元路由与桌面/手机组件测试；数据库验证授权集合和正式表格。 |
| 6 React 检查 | 帮助中心 try/catch 内构造 JSX；收藏 effect 同步命中缓存后 setState。 | JSX 移出读取 catch；收藏异步结果统一处理，不通过抑制 lint 掩盖。 | lint 通过；初始收藏不新增读取，回到页面仅执行受保护的复核。 |
| 7 上线门槛 | 仓库没有 GitHub workflow；Vercel 配置仅区域和性能日志，未规定质量门槛。 | 新增 Verify workflow：类型、lint、单元、构建、关键浏览器测试；Vercel 构建改用 `verify:build`。 | 本地运行同一套命令。无法确认远端分支保护、Vercel 控制台覆盖项或部署检查绑定；CI 文件尚未推送运行。 |
| 8 状态接口 | readiness 永远 503/not_integrated；health 仅进程响应且无版本。 | 两接口返回构建提交标识；readiness 探测公开签名公钥、两类数据库连接的最小权限角色和必要结构，限定超时，15 秒合并探测；无业务数据写入。 | 模拟依赖测试验证连接关闭、并发合并、失败脱敏与不发送登录密钥。明确 `authenticationScope=public_signing_keys_only`、`loginVerified=false`；不能证明真实登录、成员授权或完整业务链路可用。 |
| 9 清除筛选 | 重置强制 `kind=all`。 | 保留当前 kind，清除搜索、状态、个人范围和页码。 | QA 清除链接继续指向 QA；浏览器与静态渲染验证。 |
| 10 筛选显示 | 原生非受控字段只在初次挂载采用 defaultValue。 | 按完整路由查询给表单 key，使导航后重新采用服务端值。 | 同一 React 根从 QA 切至 Reference，下拉显示同步；不依赖整页刷新。 |
| 11 后台重试 | 没有 data 时模块身份丢失，重试固定 `/admin`。 | 独立传入查询及重试 URL，保留模块、筛选及视图。 | QA 失败页标题和重试查询浏览器验证。 |
| 12 正式版本 | 阅读端已有 publicationNumber，编辑器/列表/首页仍使用物理 revision。 | 编辑器、工作台、最近阅读从现有发布次数函数取值；未知次数只显示已发布。历史/审核中的物理号明确标为工作修订，不再冒充正式版号。 | 数据库验证多次工作修订后首次发布仍是正式版 1；编辑器旧正式版可读回归；全站对应文案核对。不改变版本/并发锁算法，不新建迁移。 |
| 13 状态数字 | 统计按当前工作版，旧 publishedRevision 仍可读，两者逻辑可同时成立。 | 状态数字旁明确说明统计工作版，草稿/审核中不代表旧正式内容下线；使用独立样式，避免旧的隐藏规则使说明不可见。 | 数据库验证 draft 与 published count=0 并存且旧正式指针保留；界面断言说明可见。 |
| 14 QA 管理文案 | 标题输入、数量沿用文章词汇；列表不显示 QA 分类和顺序。 | 使用搜索问题、输入问题、个问题；列表/看板显示问答分类与排序，说明数字越小越靠前；说明在问答设置编辑。 | 渲染/浏览器测试验证 QA 文案与模块路由；数据库投影读取 QA 元数据。 |
| 15 首次加载 | 身份未加载时 AuthenticatedQaAnswer 返回空；账号栏把未确认显示为未登录。 | 问答与账号显示正在确认；新增路由加载页面；身份不匹配时明确要求刷新而非展示旧内容。 | 未加载组件渲染测试及 QA 浏览器加载/展开测试。真实账号初次访问的网络时序仍需线上确认。 |
| 16 QA 读取失败 | 答案失败统一提示无法读取或没有权限。 | 区分网络/超时、未登录、权限不足、不存在和服务错误；提供检查网络、重新登录、联系管理员或重读。明确拒绝访问时清除旧答案。 | 浏览器模拟断网、403、503；权限撤回清除与服务暂不可用保留已读答案的原有测试继续覆盖。 |
| 17 搜索开关 | QA 搜索表单未读取功能开关，但服务端已有搜索检查。 | 页面读取服务端开关并传给 QaView；关闭时隐藏表单、忽略旧 URL 搜索词，显示可阅读问答；保留后端校验。 | 搜索表单显隐回归；数据库开关权限逻辑保持原有检查。 |
| 18 入口含义 | 首页和卡片同名但分别进入首页/文章目录；首页底部再出现后台入口。 | 文章目录卡片明确为“知识文章”；首页快捷入口保持首页含义；移除底部重复后台入口，保留账号区的授权后台入口及空内容创建指引。 | 首页组件渲染核对；没有改变目的页的服务端权限。 |

已知正常现象：后台 20 条草稿、前台 1 条已发布问答不代表内容丢失，本次未改变只向员工展示已发布内容的规则。

## 上线前仍需验证

1. 用真实员工、运营、管理员账号检查 OPS、Reference、收藏切换与权限撤回、QA 首次加载，以及真实双标签冲突。
2. 将本分支改动推送后，确认 GitHub Verify 实际运行成功；在远端设置中要求其通过才能合并/发布。当前无法确认外部保护规则。
3. 部署后核对 `/api/health` 和 `/api/readiness` 的 release 与提交一致，并逐页做授权验收。readiness 的 200 不能代替这些验收。
4. Clerk 的带服务密钥探测曾被自动审批拒绝，已改为不发送密钥的公开公钥探测。没有绕过拒绝，也没有声称密钥或真实登录已验收。
