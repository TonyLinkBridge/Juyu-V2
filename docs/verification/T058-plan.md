# T058 本地规模验收计划

范围：实际本地代码/临时 PostgreSQL/本地 Chromium；无需真实 Clerk、Supabase，不写真实账号或云端资料。当前目录非 Git 仓库，使用 /private/tmp/juyu-t058/baseline 备份；不使用 subagent。

- [x] 盘点并固化现有限制；构造 300 内容块、大目录和 200×8 表格样例。
- [x] 测浏览器载入、编辑/保存、表格筛选及目录操作；记录 5 次观测，不冒充正式部署性能。
- [x] 临时数据库构造 1000 普通正式、200 OPS、100 管理员正式、100 草稿资料；测试三角色中文搜索/目录，及两管理员真实事务并发保存。
- [x] 上传实测各类型上限的本地流式读写、超限、取消、损坏回读与忙碌恢复；存储模拟，不测云端带宽。
- [x] 实际本地 PDF 导出大文章/长表格，测超限和并发繁忙；查看首/中/尾页与文字结尾。
- [x] 对确认的问题按证据修复并复测；归档结果、更新任务与下一步。

先定本地验收预算（工程检查阈值，不是服务承诺）：读者大目录/正文载入≤3秒、编辑器可编辑≤5秒、输入后本地模拟保存确认≤5秒、表格筛选≤1秒；实际数据库单次搜索≤1秒/目录≤3秒、两人并发事务≤5秒；无网络上传校验回读≤3秒、PDF≤20秒。采样少，不称统计 p95。记录全部样本和中位数/最大值；超过预算必须说明或解决，不事后抬高预算使其通过。浏览器现有 helper 为 development 打包，构建时间排除，页面脚本执行包括在测量内。真实部署另验。

执行补充：发现上传内存重复复制后，追加冷启动独立进程512MiB本地诊断预算，旧版本红测失败、优化后通过；不代表512MiB云端实例可承载。真实服务保持待办，最终结果见 `2026-09-10-performance-scale.md`。
