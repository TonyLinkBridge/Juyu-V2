# T014 · 独立后台入口本地验收

日期：2026-09-08。项目 /Users/tony/Documents/ChatGPT/Juyu V2。

## 已完成

- 独立后台登录、错误重试、拒绝访问和当前会话退出入口；前后台共用 Clerk 账号。
- 公司身份验证后读取当前服务端用户角色；只允许 admin，拒绝 Support/Ops、未知角色、禁用或锁定账号、邮箱变化及伪造身份字段。
- 后台页面和现有后台 API 各自检查权限；员工页仅管理员显示后台链接。
- 固定登录跳转并清除覆盖参数；后台成功入口保留明确的工作台准备状态，业务资料访问仍默认关闭。

## 本地证据

- npm test：100/100 通过，0 失败、0 跳过；本任务新增 9 项管理员测试和 1 项后台退出测试。
- npm run test:e2e：20/20 通过，桌面 10、手机 10，在生产构建上验证未配置身份时的真实 HTTP/页面行为。
- npm run build、npm run typecheck、npm run lint：全部通过。
- 权限测试覆盖公司未验证时不调用角色加载器、最新角色变更、错误来源、账号/邮箱状态、状态码和操作未执行；角色身份采用测试替身，不是真实 Clerk 账号。
- 浏览器覆盖独立后台回调、拒绝页和错误重试，以及伪造身份不能放行状态和文章接口；登录覆盖参数清理保留 OAuth 参数。
- 使用实际安装的 Clerk RedirectUrls 实现测试重定向优先级，不需要真实账号或网络。
- 桌面与手机拒绝页截图已保存；人工查看 `output/verification/admin-denied-mobile.png`，文字、按钮及页脚可读，无横向溢出。桌面截图不计为人工视觉验收。
- 数据库实现未改，本轮未重跑数据库测试；以前的数据库通过数量不计为新证据。

## 独立审查与修复

审查发现 Clerk 的查询参数可覆盖组件配置的固定登录目标，已用安装 SDK 复现同源错误跳转。先添加回归并观察失败，再加入登录地址参数清理和明确的允许来源；回归、全套测试及复核通过，审查项关闭。OAuth 所需参数保留。没有把同源复现扩大为已证明任意外站跳转。

## 尚未完成

Clerk 测试项目以及实际公司邮箱域名、Workspace ID 尚未配置。真实公司准入、管理员/员工登录退出、OAuth 回调和实际角色变化仍需联合验收；T014 整体保持进行中，仅本地实现完成。

后台编辑页面、成员管理、首位管理员引导和业务身份到受限数据库的绑定不在本任务内。没有部署，没有修改真实账号或元数据。下一项 T015：Clerk 角色与成员管理。

接入说明：`src/server/authentication/ADMIN.md`。
