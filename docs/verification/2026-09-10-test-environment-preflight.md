# T059 · 本地配置准备验证

日期：2026-09-10。用户确认专用 Clerk/Supabase 测试项目已准备，会在本地填写配置。本次没有连接、初始化或修改真实项目，没有使用 subagent。

## 变更

- 新增 `npm run check:config`，默认 production，`--development` 对应开发模式；使用当前 Next 依赖中的环境加载器，遵循应用配置优先级。
- 复用 Clerk、数据库、公司准入和 Storage 构造校验；输出仅变量名和固定状态，不输出值和异常详情。
- Clerk live 密钥不允许用于本轮测试预检；格式通过只标记可进入连通检查，始终注明真实检查未运行。
- 原子创建空白 `.env.local`，权限 0600；已有文件不会覆盖。已请求在 Codex 文件面板打开。
- 新增当前接入说明，历史认证/数据库/存储/成员/开通文档增加当前说明入口；没有修改旧迁移和应用授权行为。

## 验证证据

1. 先运行新增测试：因检查模块尚不存在而失败；随后实现模块，新增 8 项全部通过。
2. 全部规则测试：334/334 通过；原始本地输出 `/private/tmp/juyu-t059-unit.log`。
3. `npm run typecheck` 与 `npm run lint` 通过。
4. 使用虚构值实际运行 CLI：production、development 均返回格式通过及 liveChecks=not_run；拒绝未知参数，错误输出没有回显参数或配置值。
5. 当前本地配置复检：blocked，8 个必填项缺失，无格式错误；退出码 1 是正确的未配置结果。

本次未改页面及应用运行逻辑，未重新构建或重启 3211 预览；没有把既有构建或浏览器结果算作本次新验证。CLI 测试与规则测试不使用真实账号。

## 未完成

真实密钥有效性、公司邮箱/Workspace、项目身份、受限数据库登录与迁移、私有桶及权限、Clerk/Slack 回调、首位管理员和三角色真实验收，均尚未验证。配置检查不能证明 Supabase URL 与密钥配对、数据库角色权限、连接池模式或生产就绪。T059 保持进行中。


## 同日更新：Clerk Production 路线

用户选择直接配置 Clerk Production，提供 GitHub 仓库地址；随后确认正式域名尚未确定，Vercel 尚未建立。仅收到仓库地址，读取远端未成功，不能认定源码已上传。

已将 .env.local 中三个有效域名的空格分隔改为英文逗号，域名本身与其余变量保持原样。未记录密钥。发现预检把缺少 Workspace ID 误报为邮箱域名错误：先添加测试复现，再移除重复的组合诊断，保留各字段独立校验。新增 --clerk-production 显式选择正式实例，要求 live 密钥及 HTTPS 自有域名；默认测试路线保留。未改应用权限和身份验证规则。

验证：新增三项测试先失败、修复后通过；337/337 规则测试通过，typecheck 与 lint 通过。使用虚构配置运行 Production CLI 返回格式通过及 liveChecks=not_run，无密钥输出。规则测试日志 /private/tmp/juyu-t059-production-unit.log。

实际本地复检：Supabase 两项已填、三域名格式通过；Clerk 两项密钥、Workspace ID、两条数据库连接缺失，APP_ORIGIN 仍为本地地址，不能通过正式域名检查。没有连接服务、部署网站或创建真实账号。当前 Vercel 大文件上传请求体限制与 Chromium 打包尚待适配，见正式接入指南。
