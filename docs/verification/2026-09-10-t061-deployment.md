# T061 正式部署验收

目标：Vercel Juyu / helpcentre，正式域名 https://juyu-helpcentre.vercel.app ，生产数据库 zscxaqjqjoouiolkoxbi。恢复项目 bgrvonvrytlaufityutq 不接入正式网站。

## 本轮发布内容

- 修复 Supabase 私有 bucket 初始化对 HTTP 400 包装 NoSuchBucket / 404 的识别，仍拒绝权限错误、公开 bucket 和覆盖文件。
- 纳入 T060 可重复演练脚本、回归测试及操作/验收记录。运维脚本不由网页或启动流程执行。
- 没有新增数据库迁移、没有修改 Vercel 密钥、没有部署恢复项目的密码。

## 发布前实际检查

343 项规则测试通过，1 项本地数据库与媒体综合恢复测试通过，lint、Next.js 正式 build 通过。
实际连接生产数据库核对 26 条迁移校验值一致；runtime 与 issuer 两个真实登录账号均通过严格 TLS 和 checkRole 权限检查。

## 发布结果

已推送 GitHub main：`59b22eaa5c99ae886b464dfa7902748ab4aa60a2`。
Vercel deployment：`6AGXZAMkKL7R2vx1icrrEiQbNHqC`，构建约 29 秒，Ready。
部署地址：https://helpcentre-mnc5mz7jo-juyu.vercel.app 。Overview 已确认正式域名绑定该部署及 commit，显示 main 分支推送更新 Production 的提示。

实际部署后检查：

- 使用现有已登录 Admin 会话访问 /admin，成功加载真实工作台，1 篇等待审核、0 篇发布；没有修改、审核或发布资料。此项是已登录会话检查，不是重新完成 Slack OAuth 登录。
- 同一会话进入 /help-centre，首页正常，显示正式资料准备中；待审核资料未作为正式文章展示。
- 独立匿名请求 /api/health 为 200 / web_process_only；/admin 和 /help-centre 流式响应包含 NEXT_REDIRECT 登录跳转，没有观察到服务端异常页。
- /design-preview/home 为 404，开发预览未开放。
- 真实 /api/assets/:id 与 /api/admin/assets/:id 匿名请求均为 403，private/no-store。没有以不存在的 /api/attachments 路由 404 作为附件保护证据。
- 密钥文件及本地原始恢复结果均在 Git ignore 范围；暂存文件逐项与本地密码/服务端密钥内容比对未发现泄漏。恢复项目凭据没有传到 Vercel。

结论：T061 发布部分完成，真实核心流程验收仍待下列保留项。正式网站已经更新，不是等待部署。

本地最终验收记录和任务状态在发布后更新，尚未再次推送，避免仅为补充记录重复触发生产构建；源码及 T060 记录已在上述发布提交中。

## 保留验收

T059 原来保留的真实二审发布、Support/Ops 权限、真实附件/在线 PDF 等仍未因此通过。T061 的“正式登录及核心流程”需要对应真实证据，不以部署 Ready 或健康接口 200 代替。自动加密异地备份排程也未配置。
