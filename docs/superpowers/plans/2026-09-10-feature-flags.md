# T052 功能开关
用户已授权下一步，明确只由当前助手完成，不使用subagent。直接使用现有非Git工程；基线/private/tmp/juyu-t052/baseline。

## 确定范围
七个可选功能 search/pdfExport/favorites/recent/feedback/analytics/forms；默认全部开启，只有没有配置记录才使用默认。管理员整体版本配置、明确影响确认、准确重试、并发冲突及输入保留。关闭影响全体角色和对应后台业务入口，核心账号/权限/设置管理不关闭。
功能关闭后入口/控件隐藏，服务器每次功能操作在数据库事务内检查当前配置；仅通过UI隐藏不算完成。已开始的请求可能完成；写入与开关提交通过共享/独占锁排序，关闭提交后的新操作拒绝。恢复开关继续使用保留的数据，不删除/重建业务资料。
表单配置/记录/处理都受forms开关；管理员从功能设置重新开启。文章PDF开关仅控制应用内文章PDF阅读与生成，不影响原始PDF附件下载或浏览器原生打印。OPS/Reference/Q&A作为已有资料类型继续按原角色/分类授权，不提供关闭内容权限的替代开关。搜索关闭不影响文章阅读；分析关闭不影响收藏/最近浏览等独立记录。

## 实现与验证
1. 严格七布尔契约；新增0024迁移，settings不可变版本、同人同请求重试、角色/同源/无缓存API；不改旧23迁移。
2. 所有对应repository及PDF服务入口检查开关，API明确FEATURE_DISABLED；菜单/正文控件/搜索输入及管理工具同步过滤。功能暂停页支持返回与重新加载，关闭不伪装空数据。
3. 后台/admin/settings/features，未保存/未知结果/冲突/重复载入备份保护；不允许自建任意开关或代码。
4. 当前助手自审，真实临时PostgreSQL/单元/电脑手机专项与全站回归/build/type/lint，保留来源和验收记录。真实Clerk/Supabase/公司准入与真机部署继续待配置。

## 本地执行结果
上述四步已完成；311单元、356数据库、560浏览器和构建/类型/lint通过。由当前助手自查，无subagent。验收与边界见 `docs/verification/2026-09-10-feature-flags.md`；真实服务与设备验收仍待办。
