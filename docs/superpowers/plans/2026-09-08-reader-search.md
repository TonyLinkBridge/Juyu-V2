# T020 顶部搜索与结果页

执行现有已批准任务。改编 GitBook SearchInput、SearchResults、SearchPageResultItem、SearchResultItem、HighlightQuery 的输入、结果、图标/标题/路径、高亮结构；替换供应商 API、AI、统计和主题依赖。

本阶段仅检索已授权目录中的正式文章标题。AuthorizationService.search 在同一受限只读事务取得已授权目录后匹配、去重、计数和分页；返回结果无正文/草稿。分类路径也来自同一目录。T041 仍负责正文、Reference、Q&A、索引更新、相关度等统一搜索。

员工页顶部 GET 表单，q 参数进入结果页，回车/提交按钮、清空/Escape、Cmd/Ctrl K 聚焦；原生 Tab/Enter 操作结果链接。中文组合输入不得误提交。空白提示输入；重复或超长 query 拒绝而不静默截断；字面匹配空格分词且全词包含；每页20条，越界页能回第一页。服务失败显示重试而非伪造零结果。查询不写入浏览器存储或分析。

- [x] 参数、中文/字面匹配、稳定去重计数/分页测试先失败，再实现。
- [x] 授权搜索服务与正式版本/角色/分类/下线数据库测试。
- [x] GitBook 搜索组件适配，员工页接入，桌面/手机键盘及状态验收。
- [x] 独立复核、回归、来源哈希与验收记录。
