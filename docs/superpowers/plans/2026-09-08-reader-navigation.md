# T017 GitBook 左侧目录实施计划

目标：按已批准 TASKS.md T017 和员工阅读端设计，实际移植 GitBook 平面文章目录，按可信身份提供已发布标题及当前项高亮。

范围：T017；分组和排序配置属于 T018，正文属于 T019，手机抽屉属于 T021。本次手机先保持可访问的内联目录，不隐藏入口。无真实服务配置时保持关闭，不建立演示身份或生产示例内容。

架构：复用 AuthorizationService 和 ScopedDatabase，在同一受限只读事务从 revisions 查询 can_read_revision 为真的标题（Admin 同样显式过滤）。客户端只收到 id、title 及固定站内 href。员工主页通过 article 查询参数选择目录项，不泄漏无权或不存在文章的区别。保留 GitBook PagesList、PageDocumentItem、ToggleableLinkItem 的文档列表分支和 styles 来源，移除供应商专用 API、追踪和未到期功能，记录原文件哈希及改动。

- [x] 先增加数据库权限/旧正式标题/下线/分类限制验收，确认缺失目录功能导致失败。
- [x] 实现服务端目录与受保护 GET 接口，禁止正文及后台字段进入响应。
- [x] 移植源码、样式适配；集成员工主页，区分空目录、加载失败和不可用文章。
- [x] 电脑/手机交互验收：选中、刷新/返回、键盘焦点、长标题滚动、空/错误状态；使用隔离测试页面，不新增认证旁路。
- [x] 复核来源、权限和页面接线；运行必要测试、构建、类型检查和 lint。
- [x] 更新来源记录、验收说明和 TASKS.md。真实 Clerk/Supabase 联合验收保持待配置。
